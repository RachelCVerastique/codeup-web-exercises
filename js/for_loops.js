

    function showMultiplicationTable(num) {
        for (let i = 1;i <= 10; i++) {
            let result = i * num;
                console.log(num + " x " + i + " = " + result);
            }
        }

showMultiplicationTable(7);